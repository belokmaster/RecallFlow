# Recall Flow - Приложение для изучения слов

Современное веб-приложение для создания и управления карточками для изучения иностранных слов с отслеживанием статистики обучения.

## ✨ Особенности

- 📚 Создание и управление карточками для изучения слов
- 📊 Отслеживание статистики (попыток, успехов, процента правильных ответов)
- 🎨 Красивый и интуитивный интерфейс на React
- ⚡ Быстрая работа благодаря React + Vite
- 🔄 Синхронизация с бэкендом в реальном времени
- 📱 Адаптивный дизайн для мобильных устройств и десктопа

## 🛠 Стек технологий

- **Frontend**: React 19 + Vite (TypeScript-ready)
- **Backend**: Go 1.22 с http.ServeMux
- **База данных**: SQLite (встроенная, не требует отдельной установки)
- **CSS**: Vanilla CSS с современными возможностями (Grid, Flexbox)

## 📋 Требования

- **Go**: 1.22 или выше
- **Node.js**: 16 или выше

## 🚀 Установка и запуск

### Шаг 1: Сборка фронтенда

```bash
cd web/frontend
npm install
npm run build
cd ../..
```

### Шаг 2: Запуск приложения

```bash
go run ./cmd/server/main.go
```

Приложение будет доступно на `http://localhost:8080`

**Примечание:** База данных SQLite (`recall_flow.db`) создается автоматически при первом запуске в корневой папке проекта.

## 💻 Разработка

Для разработки с горячей перезагрузкой откройте 2 терминала:

**Терминал 1 - Frontend dev server:**
```bash
cd web/frontend
npm run dev
```
Frontend будет на `http://localhost:5173`

**Терминал 2 - Backend server:**
```bash
go run ./cmd/server/main.go
```
Backend на `http://localhost:8080`

API запросы с фронтенда будут автоматически проксироваться к бэкенду через Vite proxy.

## ℹ️ О базе данных

- **SQLite** - встроенная база данных
- **Файл**: `recall_flow.db` (создается автоматически в корне проекта)
- **Сброс данных**: просто удалите файл `recall_flow.db`
- **Никакой дополнительной настройки не требуется!**

## 📚 API Endpoint'ы

### Карточки

| Метод | Путь | Описание |
|-------|------|---------|
| `GET` | `/cards` | Получить все карточки |
| `POST` | `/cards` | Создать новую карточку |
| `PUT` | `/cards/{id}` | Обновить карточку |
| `DELETE` | `/cards/{id}` | Удалить карточку |

### Структуры данных

**Card (Карточка):**
```json
{
  "id": 1,
  "word": "Hello",
  "translation": "Привет",
  "example": "Hello, how are you?",
  "created_at": "2026-02-20T14:00:00Z",
  "last_reviewed": "2026-02-20T15:30:00Z",
  "attempts": 10,
  "successes": 8
}
```

**Создание карточки (POST /cards):**
```json
{
  "word": "Hello",
  "translation": "Привет",
  "example": "Hello, how are you?"
}
```

## 🏗 Структура проекта

```
RecallFlow/
├── cmd/
│   └── server/
│       └── main.go                 # Точка входа Go приложения
├── internal/
│   ├── config/
│   │   ├── config.go
│   │   └── config_type.go
│   ├── database/
│   │   ├── db.go                   # Функции работы с БД
│   │   └── db_type.go              # Структуры данных
│   └── handlers/
│       ├── handlers.go             # HTTP обработчики
│       └── handlers_type.go        # Типы запросов/ответов
├── web/
│   └── frontend/
│       ├── src/
│       │   ├── components/
│       │   │   ├── CardsList.jsx   # Список карточек
│       │   │   ├── CreateCardModal.jsx # Modal создания
│       │   │   └── ViewCardModal.jsx   # Modal просмотра
│       │   ├── App.jsx             # Главный компонент
│       │   ├── App.css             # Стили приложения
│       │   ├── index.css           # Глобальные стили
│       │   └── main.jsx            # Входная точка React
│       ├── public/                 # Статичные файлы
│       ├── dist/                   # Собранное приложение
│       ├── index.html              # HTML шаблон
│       ├── vite.config.js          # Конфиг Vite
│       └── package.json            # Зависимости
├── go.mod                           # Зависимости Go
├── text.txt                         # Конфиг БД (создать самому)
├── app                             # Скомпилированное приложение
└── README.md                        # Этот файл
```

## 🔄 Workflow приложения

1. **Загрузка карточек**: При старте приложение загружает все карточки с сервера
2. **Создание карточки**: Пользователь вводит слово, перевод и опционально пример
3. **Просмотр карточки**: Клик на карточку открываетModal с информацией и статистикой
4. **Удаление карточки**: Из Modal'а можно удалить карточку

## 🐛 Решение проблем

### Ошибка подключения к БД
- Проверьте что PostgreSQL запущена
- Убедитесь что `text.txt` содержит корректные учетные данные
- Проверьте что пользователь и база данных существуют

### Frontend не отображается
- Убедитесь что вы запустили `npm run build` в папке `web/frontend`
- Проверьте что файлы в `web/frontend/dist` существуют

### CORS ошибки
- Убедитесь что оба сервера (React dev и Go) запущены
- Проверьте что API запросы используют правильные пути

## 📝 Примечания развития

В будущем можно добавить:
- 📖 Режим обучения (flashcard режим)
- 🎯 Спaced repetition алгоритм
- 🌐 Синхронизация между устройствами
- 💾 Экспорт/импорт карточек
- 🎨 Темы оформления
- 👥 Мultiple пользователи и синхронизация- 🔄 Миграция с PostgreSQL на SQLite выполнена успешно! Теперь приложение не требует установки отдельного сервиса БД
## 📄 Лицензия

MIT - Свободное использование в образовательных целях
